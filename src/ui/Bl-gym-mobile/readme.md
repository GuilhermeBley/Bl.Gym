# Bl Gym visual project for mobile applications

## How to execute the project

### Environments variables

First of all, in this project, it's required to add some values to the environment variables.

Create an '.env' file and add the following values:

API_BASE_URL=blgymtest.azurewebsites.net